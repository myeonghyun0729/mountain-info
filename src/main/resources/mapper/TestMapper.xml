<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.toy.restarea.mapper.TestMapper">
    <insert id="insertTest" parameterType="com.toy.restarea.dto.TestDto"
            keyProperty="id">
        INSERT INTO test (title)
        VALUE ( #{title} )
    </insert>


</mapper>